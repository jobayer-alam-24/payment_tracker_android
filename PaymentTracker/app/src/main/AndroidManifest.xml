<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PaymentTracker">
        <activity
            android:name=".StudentPaymentActivity"
            android:exported="false" />
        <activity
            android:name=".ViewStudentsActivity"
            android:exported="false" />
        <activity
            android:name=".CreateStudentActivity"
            android:exported="false" />
        <activity
            android:name=".BatchDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".StudentsActivity"
            android:exported="false" />
        <activity
            android:name=".BatchesActivity"
            android:exported="false" />
        <activity
            android:name=".CreateBatchActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".DailyReminderReceiver" />
    </application>

</manifest>